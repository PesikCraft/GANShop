{
  "name": "offline-shop-json-server",
  "version": "1.0.0",
  "private": true,
  "description": "File-based JSON server for offline shop sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}

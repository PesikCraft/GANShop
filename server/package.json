{
  "name": "offline-shop-json-server",
  "version": "1.1.0",
  "private": true,
  "description": "File-based or Postgres-backed JSON server for offline shop sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
